# Matching server protocol source

This is the `nh_thin_bridge/src` snapshot used by the Android client imported on 2026-09-22. It is retained for protocol traceability and is excluded from the iOS client JAR/IPA. Build it in the server project with Java 17 and the matching Paper 1.20.1 API. No server files, plugins or services are modified by the iOS build.
